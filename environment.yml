name: value-steering-bandit
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - torch>=2.6
      - transformers>=5.12
      - huggingface_hub>=0.34
      - accelerate>=1.2
      - pytest
      - pytest-cov
      - pandas
      - pyarrow
      - numpy
      - scipy
      - scikit-learn
      - matplotlib
      - pyyaml
      - black
      - ruff
